import React, { useRef, useEffect } from "react";
import { select } from "d3";

function Piechart() {


  return(
      <React.Fragment>
      <svg ref={svgRef}>
      </svg>
      </React.Fragment>
  )
}

export default Piechart